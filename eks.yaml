apiversion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: roboshopt-dev
  region: us-east-1
managedNodeGroups:
  - name: roboshop-dev
    instanceTypes: ["m5.large","t3.medium"]
    desiredCapacity: 2  # default is 3
    spot: true
