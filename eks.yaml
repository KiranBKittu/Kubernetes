apiversion: eksctl.io/v1alpha5
kind: ClusterConfig
metadata:
  name: roboshopt-dev
  region: ap-south-1
managedNodeGroups:
  - name: roboshop-dev
    instanceTypes: ["m5.large","t3.micro"]
    desiredCapacity: 2  # default is 3
    spot: true
